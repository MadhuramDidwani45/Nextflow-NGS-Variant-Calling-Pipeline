params {
    // Input
    sample = "data/*.sra"
    reads = "reads/*_{1,2}.fastq.gz"
    fastqc_result = "results/Fastqc/Before"
    trimmed_reads = "reads/trimmed"
}

// Resource profiles
profiles {
    standard {
        process.cpus   = 2
        process.memory = '4 GB'
    }
}
